# Optional: absolute URL for Kanban API payloads.
# Leave empty to use the built-in local mock endpoint.
VITE_KANBAN_API_URL=

# Optional: simulate slow network responses in milliseconds (0-15000).
VITE_KANBAN_API_DELAY_MS=0
